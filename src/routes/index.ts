import { Router } from 'express';
// import authRoutes from './authRoutes';
// import exerciseRoutes from './exerciseRoutes';
// import userRoutes from './userRoutes';

const router = Router();

// Đăng ký các routes chính
// router.use('/auth', authRoutes);  // Các route liên quan đến authentication
// router.use('/exercises', exerciseRoutes);  // Các route bài tập
// router.use('/users', userRoutes);  // Các route người dùng

export default router;
